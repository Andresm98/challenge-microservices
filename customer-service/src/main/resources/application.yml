server:
  port: 8081

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/customer_db
    username: user
    password: password
  sql:
    init:
      mode: always
  kafka:
    bootstrap-servers: localhost:9092